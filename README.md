Simple proof of concept around reactive, k8s API and kotlin.


Idea was to use stream to get some object and interact with them.


This is just a reactive stream that get k8s namespace event (watch namespace).